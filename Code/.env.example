# Web3Forms access key for the contact form (Code/src/components/ContactForm.vue).
# Get one at https://web3forms.com — public by design, safe to expose to the browser.
# Leave unset locally to see the "not set up yet" fallback instead of a working form.
PUBLIC_WEB3FORMS_ACCESS_KEY=

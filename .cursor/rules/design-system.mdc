---
description: Design-system rules for token-driven styling
globs: Code/src/**/*.astro, Code/src/**/*.vue, Code/src/**/*.css, Code/tokens/**/*.mjs, Code/scripts/**/*.mjs, **/tokens/**/*.mjs, **/styles/**/*.css
alwaysApply: false
---

Auto-attaches when editing components, styles, or token pipeline files. Before editing,
read the durable guidance:

- Styling conventions, deviation handling, image handling: `Agents/context/design-system.md`
- Token pipeline, opacity/alpha guard, color modes: `Agents/context/design-tokens.md`

Keep this rule thin: put durable guidance in `Agents/` and reference it here rather than
duplicating it.
